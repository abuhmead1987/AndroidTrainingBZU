<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.examples.android.androidtrainingbzu">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:theme="@style/StyledActivity" android:name=".NameActivity"></activity>
        <activity android:theme="@style/StyledActivity" android:name=".ForResultsActivity" />
        <activity android:theme="@style/StyledActivity" android:name=".ActivityLifcycle" />
        <activity android:theme="@style/StyledActivity" android:name=".LifeCycleOfActivity" />
        <activity android:theme="@style/StyledActivity" android:name=".LongTextActivity" />
        <activity android:name=".MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>